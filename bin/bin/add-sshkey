#!/bin/bash

TIMEOUT="1h"
NAME=$1

if [ -z "$NAME" ]; then
    ssh-add -L
else
    if [ -f "~/.ssh/id_ed25519_${NAME}" ]; then
        ssh-add -t $TIMEOUT "~/.ssh/id_ed25519_${NAME}"
    else
        echo "No key named ${NAME} found..."
    fi
fi